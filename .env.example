# Documentation only: the CLI reads the process environment, not this file.
# Run only on loopback. No external model credentials are needed.
DAIA_DB=.runtime/daia.sqlite3
# A dev contributor token may be read by a trusted MCP host from its environment.
# Generate a fresh token locally with: python -m daia.cli invite
DAIA_TOKEN=
