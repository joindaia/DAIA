# Raw-protocol template. Prefer scripts/connect-codex.ps1 and docs/desktop-contributor.md
# for the local helper with automatic signing, consent bounds and recovery.
# MCP 2.2.0 protocol and Codex CLI discovery tested; see docs/tailnet-pilot.md.
# Add this section to the host's private config; keep the token in its environment.
# No OAuth login endpoint is implemented in the development coordinator.
[mcp_servers.daia]
url = "http://127.0.0.1:8000/mcp"
bearer_token_env_var = "DAIA_TOKEN"
